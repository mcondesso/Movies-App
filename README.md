# Movies App

## Installation

To install this project, simply clone the repository and install the dependencies in requirements.txt using `pip`

Also, make sure to add a .env file containing  the API Key you obtained from the Omdb API (https://www.omdbapi.com/). The .env file should look like this:
```
API_KEY=<my_api_key>
```
